apiVersion: apps/v1
kind: Deployment
metadata:
  name: html-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: html-app
  template:
    metadata:
      labels:
        app: html-app
    spec:
      imagePullSecrets:
      - name: dockerhub-creds
      containers:
      - name: html-app
        image: huytm1996/html-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: html-service
spec:
  selector:
    app: html-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 31334
  type: NodePort