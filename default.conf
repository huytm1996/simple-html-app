# Server trên port 80
server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index1.html;
    }
}

# Server trên port 8081
server {
    listen 8081;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index2.html;
    }
}
